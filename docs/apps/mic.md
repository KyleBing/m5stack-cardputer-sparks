# Mic 麦克风

Hardware Test 第 2 页按键：`1`（主菜单 `h` → `[`/`]` 翻到第 2 页 → `1`）

实时示波器波形 + VU 电平 + 增益调节。v1.0.0 起**不再**支持 TF 录音。

## 截图

<div class="shot-row">

![mic-001](/shots/app_mic_001.png)
![mic-002](/shots/app_mic_002.png)

</div>

## 快捷键

| 按键 | 作用 |
|------|------|
| `-` `=` 或 `,` `.` | 增益 − / + |
| `h` | Help（进 Help 会关麦；关闭 Help 后恢复） |

## 使用说明

1. 进入即开麦画波形；注意环境噪声与增益，避免削波。
2. 退出时会静默回收 Speaker / I2S，减轻嗡嗡声。
3. 录音相关功能已移除；只需看电平与波形时用本 App。
4. 在 Hardware Test 子项中按 `ESC` / `GO` 返回 Test 顶层。
