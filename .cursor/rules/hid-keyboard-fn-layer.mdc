---
description: Keyboard (HID) app Fn layer mappings and usage conventions
globs: src/app_hid_keyboard.cpp,include/app_hid_keyboard.h
alwaysApply: false
---

# Keyboard（`k` 应用）Fn 层约定

库不会自动做 Fn 映射；`app_hid_keyboard.cpp` 在组 HID report 时自行转换。

## 面板橙色 / 功能键

| 组合 | HID |
|---|-----|
| \`（裸按） | Esc（本 App 用 BtnGO 退出，不占 Esc） |
| Fn + \` | Esc（兼容） |
| Fn + Bksp | Delete |
| Fn + ; , . / | ↑ ← ↓ → |
| Fn + 1..0 | F1..F10 |
| Fn + - / = | F11 / F12 |

未列出的 Fn+键：吞掉，不发给主机。

## IMU 鼠标

| 操作 | 作用 |
|---|-----|
| 长按 Fn | 开关 IMU→主机鼠标指针（需板载 IMU） |
| 左半字母 qwerty/asdfg/zxcv（ygv 属左） | 左键 |
| 右半字母 uiop/hjkl/bnm（uhb 属右） | 右键 |
| 1–9 / 0 | 灵敏度 1..10（Fn 按下时留给 F 键） |

IMU 开启时：字母不作字符发给主机（作点击）；数字调灵敏度；其余功能键（修饰 / Esc / Enter / Fn 层等）仍可发给主机。主界面无 header；左侧胶囊列 Fn/Aa/Opt/Ctrl/Alt；IMU 时居中鼠标图标 + 右侧灵敏度条；非 IMU 时居中大号按键回显。

## 左下角修饰键（Ctrl / Opt / Alt）

| 组合 | 含义 |
|---|-----|
| Ctrl / Opt / Alt | Left Ctrl / Left GUI(Win·Cmd) / Left Alt |
| Fn + Ctrl / Opt / Alt | Right Ctrl / Right GUI / Right Alt |
| Fn + Aa(Shift) | Caps Lock（并翻转本地 caps 状态） |

## 应用热键（不发给主机）

| 组合 | 作用 |
|---|-----|
| Fn + u / b | USB / BLE |
| Fn + p | BLE 主机列表（最多 5 槽：切换 / 新配对 / 删除） |
| Fn + h | 帮助页（`,` / `.` 翻页，`h` 关闭） |
| BtnA | 退出应用（裸 \` 发 Esc 给主机） |

### 主机列表内按键

| 按键 | 作用 |
|---|-----|
| 1–5 / 方向键 | 选槽 |
| Enter / Space | 切换到该主机 |
| n | 开放新配对（需空槽；会拒绝旧主机抢连） |
| r | 重命名别名（Enter 保存，空名回退 MAC，` 取消） |
| d | 删除槽位 bond |
| h / p | 关闭列表 |

## 注意事项

- 默认 BLE，避免占用 USB 烧录口；USB 模式退出后需恢复 Serial/JTAG
- 退出 Keyboard（BtnGO / 回菜单）会断开 BLE 并 deinit 协议栈；先全屏 Exiting，期间禁止刷 header 蓝牙图标
- Header 蓝牙图标看 `BLEDevice::getInitialized()`（含本应用自建栈）；主输入 / Help / Hosts / Exiting 均不画 header
- BLE 同时只连一台主机；多设备靠 `Fn+p` 列表切换；Hosts：左侧 5 槽卡片，右侧竖排 tip，左下角配对状态
- 槽位 NVS 名仍为 `hidkb`（兼容旧数据）
- BLE 复合键鼠：PnP 版本已 bump；旧配对主机需忘掉后重配才能收到鼠标报告
- 帮助文案变更时同步改 `drawHelpPage()`；Hosts 无独立 Help 页
